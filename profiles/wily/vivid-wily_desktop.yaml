---
- testname: "Vivid->Wily desktop upgrade"
  backend: lxc

  # I think this section should be a list so it's possible to start at precise,
  # go to trusty & test then upgrade to the next and test, all without starting
  # afresh.
  test-details:
    start-release: "vivid"
    end-release: "wily"
    arch: amd64
    pre_upgrade_tests:
      - tests/lts-desktop/pre
    post_upgrade_tests:
      - tests/lts-desktop/post
